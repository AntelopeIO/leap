#pragma once
#include <fc/crypto/bls_utils.hpp>

namespace fc { namespace crypto { namespace blslib {

      static bool verify(  const blslib::bls_public_key &pubkey,
                           const vector<uint8_t> &message,
                           const bls_signature &signature){

      }
      
} } }  // fc::crypto::blslib
